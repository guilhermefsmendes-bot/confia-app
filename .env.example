# Production hardening (optional locally, required for enforced App Check)
# Register reCAPTCHA Enterprise for the Firebase App Check web app first.
VITE_FIREBASE_APPCHECK_RECAPTCHA_SITE_KEY=

# Optional privacy-preserving error collector. Leave empty to keep errors local.
VITE_ERROR_REPORT_URL=
