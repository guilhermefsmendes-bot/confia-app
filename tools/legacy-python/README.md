# Legacy automation scripts

These Python files are historical audit, migration, and one-off editing utilities used during Confia development.

They are intentionally kept outside the application root so the production codebase is easier to inspect and maintain.

They are not part of the web application runtime or build pipeline.

Before reusing a script, review it against the current source tree and run it in a disposable working tree.